(function() {
    const scanButton = document.createElement('button');
    scanButton.textContent = 'Scan';
    scanButton.style.position = 'fixed';
    scanButton.style.top = '10px';
    scanButton.style.right = '10px';
    scanButton.style.zIndex = '9999';
    document.body.appendChild(scanButton);

    const resetButton = document.createElement('button');
    resetButton.textContent = 'Reset';
    resetButton.style.position = 'fixed';
    resetButton.style.top = '40px';
    resetButton.style.right = '10px';
    resetButton.style.zIndex = '9999';
    document.body.appendChild(resetButton);

    let inputs = [];

    scanButton.addEventListener('click', () => {
        // Ensure inputs are found and updated
        inputs = Array.from(document.querySelectorAll('input[type="text"], input[type="number"]'));
        if (inputs.length > 0) {
            inputs.forEach(input => input.value = 'Answer');
        } else {
            console.log('No inputs found to update.');
        }
    });

    resetButton.addEventListener('click', () => {
        if (inputs.length > 0) {
            inputs.forEach(input => input.value = '');
            inputs = [];
        } else {
            console.log('No inputs to reset.');
        }
    });
})();
